void main()
{
	int 1n;		// Invalid: A variable name shouldn't start with a number

	int _1n;	// Valid: A variable name can begin with
				// an underscore '_' or an alphabet

	int n1;		// Valid: A number can appear after first character

	int emp code; // Invalid: An identifer name shouldn't contain
				  // white space.

	int emp_code; // Valid: Use '_' to simulate presence of space

	int empCode; // Valid: Use mixedcase letters to represent
				 // break in the words

	double int;  // Invalid: An identifer name shouldn't be
				 // a keyword.

	int a, b, c;	// Valid: You can define multiple variables at
					// a time.
	int d, e = 1, f; // Valid: Initialization is optional, however\
					// It's a good practice to always initialize
					// a variable.

}